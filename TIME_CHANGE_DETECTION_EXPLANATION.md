# 🔍 شرح نظام اكتشاف تغيير الوقت/التاريخ

## 📋 نظرة عامة

هذا النظام يكتشف عندما يقوم أي مستخدم (DM/FT/PM/MSL) بتغيير تاريخ أو وقت الجهاز أثناء إرسال تقرير كوتشينج، ويرسل إشعار تلقائي لجميع General Managers.

---

## ⚙️ كيف يعمل النظام؟

### **الخطوة 1: عند إرسال التقرير (Submit Report)**

عندما يقوم أي مستخدم بإرسال تقرير كوتشينج:

```dart
// في lib/services/supabase_service.dart - دالة saveReport()
await client!.from('reports').insert(reportData).select();

// بعد حفظ التقرير بنجاح، يتم التحقق من التاريخ
final isTimeManipulated = checkTimeDateManipulation(report.date);
```

---

### **الخطوة 2: التحقق من التاريخ (Check Time/Date)**

الدالة `checkTimeDateManipulation()` تقوم بـ:

#### **1. أخذ التاريخ الحالي للجهاز:**
```dart
final now = DateTime.now(); // مثال: 2024-01-15 14:30:00
```

#### **2. أخذ تاريخ التقرير:**
```dart
final reportDateTime = DateTime.tryParse(reportDate); // مثال: 2024-01-20
```

#### **3. مقارنة التواريخ (بإهمال الوقت):**
```dart
// فقط التاريخ بدون الساعة
final nowOnly = DateTime(2024, 1, 15);      // اليوم الحالي
final reportDateOnly = DateTime(2024, 1, 20); // تاريخ التقرير

// حساب الفرق بالأيام
final difference = reportDateOnly.difference(nowOnly).inDays;
// النتيجة: +5 أيام (في المستقبل)
```

#### **4. تحديد إذا كان هناك تغيير مشبوه:**

**🚨 حالة 1: التاريخ في المستقبل (أكثر من يوم واحد)**
```dart
if (difference > 1) {
  // مثال: اليوم 15 يناير، والتقرير بتاريخ 20 يناير
  // الفرق = 5 أيام → مشبوه! ✅ يرسل إشعار
  return true;
}
```

**🚨 حالة 2: التاريخ في الماضي (أكثر من 7 أيام)**
```dart
if (difference < -7) {
  // مثال: اليوم 15 يناير، والتقرير بتاريخ 5 يناير
  // الفرق = -10 أيام → مشبوه! ✅ يرسل إشعار
  return true;
}
```

**✅ حالة 3: التاريخ طبيعي (نفس اليوم أو خلال 7 أيام ماضية)**
```dart
// مثال: اليوم 15 يناير، والتقرير بتاريخ 15 يناير (نفس اليوم)
// مثال: اليوم 15 يناير، والتقرير بتاريخ 10 يناير (5 أيام ماضية)
// → طبيعي! ❌ لا يرسل إشعار
return false;
```

---

### **الخطوة 3: إرسال الإشعار (Send Notification)**

إذا تم اكتشاف تغيير مشبوه (`isTimeManipulated == true`):

#### **1. جلب جميع General Managers:**
```dart
final gms = await getAllGMs(); // جلب جميع GMs من قاعدة البيانات
```

#### **2. إنشاء إشعار لكل GM:**
```dart
final notifications = gms.map((gm) => {
  'recipient_id': gm['id'],           // ID الـ GM
  'sender_id': senderId,               // ID الـ Coach (DM/FT/PM/MSL)
  'sender_name': senderName,           // اسم الـ Coach
  'sender_role': senderRole,           // الرول (dm/ft/pm/msl)
  'notification_type': 'time_change',  // نوع الإشعار
  'title': 'Time/Date Change Detected',
  'message': 'أحمد محمد (District Manager) changed the device time/date while submitting a coaching session on 2024-01-20',
  'report_id': reportId,               // ID التقرير
  'read': false,                       // غير مقروء
}).toList();
```

#### **3. حفظ الإشعارات في قاعدة البيانات:**
```dart
await client!.from('notifications').insert(notifications);
```

---

## 📊 أمثلة عملية

### **مثال 1: تغيير التاريخ للمستقبل ❌**

**السيناريو:**
- اليوم الحقيقي: **15 يناير 2024**
- المستخدم غير تاريخ الجهاز إلى: **20 يناير 2024**
- أرسل تقرير بتاريخ: **20 يناير 2024**

**النتيجة:**
```
الفرق = 20 - 15 = +5 أيام
5 > 1 → ✅ تغيير مشبوه!
→ يتم إرسال إشعار لجميع GMs
```

---

### **مثال 2: تغيير التاريخ للماضي ❌**

**السيناريو:**
- اليوم الحقيقي: **15 يناير 2024**
- المستخدم غير تاريخ الجهاز إلى: **5 يناير 2024**
- أرسل تقرير بتاريخ: **5 يناير 2024**

**النتيجة:**
```
الفرق = 5 - 15 = -10 أيام
-10 < -7 → ✅ تغيير مشبوه!
→ يتم إرسال إشعار لجميع GMs
```

---

### **مثال 3: تاريخ طبيعي ✅**

**السيناريو:**
- اليوم الحقيقي: **15 يناير 2024**
- المستخدم أرسل تقرير بتاريخ: **15 يناير 2024** (نفس اليوم)

**النتيجة:**
```
الفرق = 15 - 15 = 0 أيام
0 ليس > 1 وليس < -7 → ❌ لا يوجد تغيير
→ لا يتم إرسال إشعار
```

---

### **مثال 4: تاريخ قديم لكن ضمن الحد المسموح ✅**

**السيناريو:**
- اليوم الحقيقي: **15 يناير 2024**
- المستخدم أرسل تقرير بتاريخ: **10 يناير 2024** (5 أيام ماضية)

**النتيجة:**
```
الفرق = 10 - 15 = -5 أيام
-5 ليس < -7 → ❌ لا يوجد تغيير (ضمن الحد المسموح)
→ لا يتم إرسال إشعار
```

---

## 🔔 متى يظهر الإشعار للـ GM؟

1. **عند فتح Dashboard:**
   - يظهر رقم أحمر على أيقونة الجرس 🔔
   - الرقم = عدد الإشعارات غير المقروءة

2. **عند الضغط على الجرس:**
   - تفتح صفحة `GMNotificationsScreen`
   - تظهر جميع الإشعارات مرتبة من الأحدث للأقدم

3. **محتوى الإشعار:**
   ```
   Title: Time/Date Change Detected
   
   Message: أحمد محمد (District Manager) changed the device 
            time/date while submitting a coaching session on 2024-01-20
   ```

---

## ⚠️ ملاحظات مهمة

### **1. النظام لا يمنع إرسال التقرير:**
- حتى لو تم اكتشاف تغيير، التقرير **يتم حفظه بنجاح**
- الإشعار يتم إرساله **بعد** حفظ التقرير
- إذا فشل إرسال الإشعار، **لا يؤثر على التقرير**

### **2. النظام يعتمد على تاريخ الجهاز:**
- يستخدم `DateTime.now()` من الجهاز
- إذا غير المستخدم التاريخ/الوقت، سيتم اكتشافه

### **3. الحدود المسموحة:**
- ✅ **نفس اليوم**: مسموح
- ✅ **غداً (يوم واحد)**: مسموح
- ✅ **أمس (يوم واحد)**: مسموح
- ✅ **حتى 7 أيام ماضية**: مسموح
- ❌ **أكثر من يوم في المستقبل**: مشبوه
- ❌ **أكثر من 7 أيام في الماضي**: مشبوه

---

## 📝 ملخص سريع

```
1. المستخدم يرسل تقرير
   ↓
2. يتم حفظ التقرير في Supabase
   ↓
3. النظام يقارن تاريخ التقرير مع التاريخ الحالي
   ↓
4. إذا كان الفرق > 1 يوم (مستقبل) أو < -7 أيام (ماضي)
   ↓
5. يتم إرسال إشعار لجميع GMs
   ↓
6. GM يرى الإشعار في Dashboard (أيقونة الجرس 🔔)
```

---

## 🔧 الملفات المتعلقة

- **`lib/services/supabase_service.dart`**:
  - `checkTimeDateManipulation()` - التحقق من التغيير
  - `sendTimeChangeNotification()` - إرسال الإشعار
  - `getAllGMs()` - جلب جميع GMs
  - `getNotifications()` - جلب الإشعارات
  - `getUnreadNotificationsCount()` - عدد الإشعارات غير المقروءة

- **`lib/screens/gm/gm_notifications_screen.dart`**:
  - صفحة عرض الإشعارات

- **`lib/screens/gm/gm_dashboard_screen.dart`**:
  - أيقونة الجرس مع العداد

- **`add_notifications_table.sql`**:
  - جدول الإشعارات في قاعدة البيانات

---

## ✅ الخلاصة

النظام يعمل **تلقائياً** عند إرسال أي تقرير:
- ✅ يتحقق من التاريخ
- ✅ يكتشف التغيير المشبوه
- ✅ يرسل إشعار لجميع GMs
- ✅ لا يؤثر على عملية حفظ التقرير

**لا يحتاج أي تدخل يدوي!** 🎉

